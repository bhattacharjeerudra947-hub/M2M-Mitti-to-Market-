/**
 * GPS Queue — REMOVED.
 * These exports are kept as stubs so existing imports don't break.
 */

export async function queueGpsPoint() { return false; }
export async function countQueuedGpsPoints() { return 0; }
export async function flushGpsQueue() { return 0; }
